{% layout 'base' %}

{% block content %}
<article>
  <header>
    <h1>{{ page.title }}</h1>
    {% if page.date -%}
      <time>{{ page.date }}</time>
    {%- endif %}
  </header>
  {{ content }}
  <footer>
    <a href="/blog">← Back to Blog</a>
  </footer>
</article>
{% endblock %}
